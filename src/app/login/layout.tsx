export const metadata = {
    title: 'FABR NETWORK - Login',
    description: 'Acesso ao painel administrativo da FABR Network',
  }
  
  export default function LoginLayout({
    children,
  }: Readonly<{
    children: React.ReactNode;
  }>) {
    return children;
  }